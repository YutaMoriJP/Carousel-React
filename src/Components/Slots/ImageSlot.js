import React from "react";
import Wrapper from "../../styles/Box";
/**
 *
 * @param {element} children - renders Image component
 * @returns
 */
const ImageWrapper = ({ children }) => {
  return <Wrapper>{children}</Wrapper>;
};

export default ImageWrapper;
