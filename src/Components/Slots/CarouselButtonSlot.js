import React from "react";
import Box from "../../styles/Box";
/**
 *
 * @param {element} children - renders button components
 * @returns
 */
const CarouselButtonSlot = ({ children }) => {
  return <Box>{children}</Box>;
};

export default CarouselButtonSlot;
